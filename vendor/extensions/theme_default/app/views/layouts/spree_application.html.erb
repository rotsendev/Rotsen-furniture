<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
<% hook :inside_head do %>
  <%= render :partial => 'shared/head'%>
<% end %>
</head>
<%
if @body_class.nil?
  if @content_for_sidebar
    @body_class = 'two-col'
  else
    @body_class = 'one-col'
  end
end
 %>
<body class="<%= @body_class %>"<%= %( id="#{@body_id}") if @body_id %>>
                                                        
   <div id="header" class="container">
    <div id="login-bar">
      <ul id="nav-bar">
           <%= render 'shared/nav_bar' %>
      </ul>
    </div>
<%= logo %>
  </div>

  <div id="header" class="container">
     <h1 style="margin:0px; padding:0px; height:151px; vertical-align:top">
      <%= image_tag "TopBanner1.jpg", :id => "imageSlide", :style => "width:96%; position:absolute" %>
     </h1>
  </div>

  <div id="header" class="container">
Please browse some of the custom pieces that we've created for our customers. We can create custom pieces for you based on our original design, your designs, or by combining elements from multiple designs. Each piece will be built to your specification, and you'll even be able to select the slabs(s) of reclaimed wood and other eco-friendly materials that we use. Please click the <strong><%= link_to 'Contact' %></strong> button above to discuss your needs or place a custom order.To browse our inventory of pieces available to be shipped immediately, please click on the <strong><%= link_to 'READY-To-SHIP', products_path %></strong> button.
</div>
  
  <div id="wrapper" class="container"> 

<% if @content_for_sidebar %>
    <div id="sidebar">
      <% hook :sidebar do %>
        <%= yield :sidebar %>
      <% end %>
    </div>
    <% end %>

    <div id="content">
      <%#= breadcrumbs(@taxon) if @taxon %>
      <% if flash[:notice] %>
      <div class="flash notice"><%= flash[:notice] %></div>
      <% end %>
      <% if flash[:error] %>
      <div class="flash errors"><%= flash[:error] %></div>
      <% end %>
      
      <%= yield %>
    </div>
    <hr>
    <%= render :partial => 'shared/footer'%>
  </div>
</body>
</html>
